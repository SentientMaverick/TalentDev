@model IEnumerable<TalentAcquisition.BusinessLogic.UpdatedDomain.EvaluationCategory>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Interview.SchedulingFinalNote)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EvaluationCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EvaluationDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deleted)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Interview.SchedulingFinalNote)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EvaluationCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EvaluationDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deleted)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
